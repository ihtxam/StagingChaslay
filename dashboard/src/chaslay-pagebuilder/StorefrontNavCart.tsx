// @ts-nocheck
'use client';

/**
 * Desktop storefront uses the flying cart on the menu page.
 * Nav-bar cart icons stay hidden so they do not duplicate that control.
 */
export function StorefrontNavCart(_props) {
  return null;
}
