import type { Request } from "express";
/** Turn a stored upload path into an absolute URL for the current shop host. */
export declare function resolvePublicAssetUrl(req: Request, path: string | null | undefined): string | null;
//# sourceMappingURL=public-url.d.ts.map