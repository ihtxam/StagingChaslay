{
  "name": "reborn-pos",
  "version": "0.1.0",
  "private": true,
  "description": "RebornPOS Windows kiosk (Tauri 2 shell around hosted WebPOS)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:nsis": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
